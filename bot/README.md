# Discord Bot

Dedicated runtime for Discord commands, events, and outbound messaging.

## Structure

- `src/commands/`: slash and text commands
- `src/events/`: Discord client event handlers
- `src/messaging/`: embeds, notifications, and outbound delivery helpers
- `src/services/`: adapters to backend APIs and Redis/pub-sub
